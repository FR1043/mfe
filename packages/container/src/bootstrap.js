import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
    <App />,
    document.querySelector('#root')
);
// for the container, you can render immediately to DOM, it's only for the other modules that you don't render